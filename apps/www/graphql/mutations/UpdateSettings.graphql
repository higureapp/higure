mutation UpdateSettings($input: UpdateUserInput!) {
    updateSettings(input: $input) {
        id
        firstname
        lastname
        email
        phone
        timezone
        locale
        language
        theme
        emailVerified
        phoneVerified
        lastLoginAt
        createdAt
        updatedAt
        avatarUrl
    }
}
